mode: ContinuousDeployment
increment: None
next-version: 1.0.0
branches:
  master:
    regex: master
    tag: 
  develop:
    regex: develop[/-]?
    tag: beta
    increment: Patch
  feature:
    regex: feature[/-]?
    tag: alpha
    increment: Minor